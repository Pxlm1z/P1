services:
  backend:
    build:
      context: ./backend
    container_name: 19-flask-backend
    env_file:
      - ./backend/.env
    ports:
      - "5019:5000"
    restart: unless-stopped
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: ${VITE_API_URL}   # << ดึงจาก .env ภายนอก
    container_name: 19-nuxt-frontend
    env_file:
      - ./frontend/.env                # ใช้ตอน runtime
    ports:
      - "3019:3000"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
